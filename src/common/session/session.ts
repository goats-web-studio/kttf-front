/**
 * Сессия пользователя.
 *
 * Здесь пока только то, на что опирается охрана маршрутов: вошёл человек или
 * нет. Состав пользователя (ТС 7.1 отдаёт его в ответе входа) приедет вместе
 * с общими DTO — до тех пор он живёт в `kttf-back` и дублировать его здесь
 * значило бы нарушить бриф 3.1. См. ОВ-10 в `docs/05-state.md`.
 */
export interface Session {
  readonly userId: string;
}
